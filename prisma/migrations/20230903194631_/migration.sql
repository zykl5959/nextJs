BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Prompt] ADD CONSTRAINT Prompt_pkey PRIMARY KEY CLUSTERED ([id]);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
